% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/get_lp.R
\name{get_lp}
\alias{get_lp}
\title{Computes scaled length at puberty}
\usage{
get_lp(pars, f = 1, lb0 = NA)
}
\arguments{
\item{pars}{5-vector with parameters: g, k, l_T, v_H^b, v_H^p}

\item{lb0}{optional scalar with initial estimate for scaled length at birth (default lb0: lb for k = 1)}

\item{eb}{optional scalar with scaled reserve density at birth (default eb = 1)}
}
\value{
scaled length at puberty (lp), scaler length at birth (lb)
and indicator equals 1 if successful convergence, 0 otherwise (info)
}
\description{
Obtains scaled length at pubertyat constant food density.
}
\details{
If scaled length at birth (second input) is not specified, it is computed (using automatic initial estimate).
If it is specified, however, is it just copied to the (second) output. Food density is assumed to be constant.
}
\examples{
get_lp(c(g = 10, k = 1, lT = 0, vHb = 0.5, vHp = 10), 1)
}
\seealso{
Other scaled get functions: \code{\link{dget_lbarb2}};
  \code{\link{fnget_lbarb2}}; \code{\link{get_lbarb2}};
  \code{\link{get_lbarb}}; \code{\link{get_lb}};
  \code{\link{get_tb}}; \code{\link{get_tm_s}};
  \code{\link{get_tp}};
  \code{\link{initial_scaled_reserve}};
  \code{\link{reprod_rate}}
}

