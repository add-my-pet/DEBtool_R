% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/reprod_rate.R
\name{reprod_rate}
\alias{reprod_rate}
\title{Gets reproduction rate}
\usage{
reprod_rate(L, f = 1, pars, Lf = NA)
}
\arguments{
\item{L}{n-vector with length}

\item{f}{scalar with functional response}

\item{pars}{9-vector with parameters: kap, kapR, g, kJ, kM, LT, v, UHb, UHp}

\item{Lf}{optional scalar with length at birth (initial value only)
or optional 2-vector with length, L, and scaled functional response f0
for a juvenile that is now exposed to f, but previously at another f}
}
\value{
list with n-vector with reproduction rates (R), scalar with scaled initial reserve (UE0),
scalar with (volumetric) length at birth (Lb), scalar with (volumetric) length at puberty (Lp) and
indicator with 1 for success, 0 otherwise (info)
}
\description{
Calculates the reproduction rate in number of eggs per time for an individual of length L
and scaled reserve density f.
}
\seealso{
Other scaled get functions: \code{\link{dget_lbarb2}};
  \code{\link{fnget_lbarb2}}; \code{\link{get_lbarb2}};
  \code{\link{get_lbarb}}; \code{\link{get_lb}};
  \code{\link{get_lp}}; \code{\link{get_tb}};
  \code{\link{get_tm_s}}; \code{\link{get_tp}};
  \code{\link{initial_scaled_reserve}}
}

